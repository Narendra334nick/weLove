{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://narendra334nick.github.io/weLove",
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "postbuild": "echo > out/.nojekyll",
    "start": "next start",
    "lint": "next lint",
    "deploy": "npm run build && npm run postbuild && gh-pages -d out"
  },
  "dependencies": {
    "next": "15.4.5",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}